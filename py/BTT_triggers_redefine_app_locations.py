# after import presets/triggers all app references stop working (even if location did not change).
# scripts/BTT/triggers.bttpreset    scripts/BTT/andSettings.bttpreset
# if importing a preset cannot fix this, do we use UI Scripting/applescript?

print("yo")